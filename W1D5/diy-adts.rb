class Stack
    def initialize
        Array.new
    end

    def push(el)
      self << el
    end

    def pop
      # removes one element from the stack
    end

    def peek
      # returns, but doesn't remove, the top element in the stack
    end
  end